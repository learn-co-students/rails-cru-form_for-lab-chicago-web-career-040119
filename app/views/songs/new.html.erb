<h1>Songs New Form: </h1>

<%= form_for @song do |s| %>
  <%= s.label :name %>
  <%= s.text_field :name %><br>

  <%= s.collection_select :artist_id, @artists, :id, :name %>
  <%= s.collection_select :genre_id, @genres, :id, :name %>

  <%= s.submit %>

<% end %>
